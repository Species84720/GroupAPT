@using Test2.Models.DBModels
@model Test2.Models.DBModels.Department

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="card managementFooter">
    <div class="card-header">
        <h5 class="card-title">Delete Department</h5>
    </div>
    <div class="card-body">

        <ul class="list-group list-group-flush">
            <li class="list-group-item">
                <dt>
                    @Html.DisplayNameFor(model => model.DepartmentName)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.DepartmentName)
                </dd>
            </li>
            <li class="list-group-item">
                <dt>
                    @Html.DisplayNameFor(model => model.DepartmentParentId)
                </dt>

                <dd>
                    @{
                        List<Department> list = ViewBag.DepartmentList;
                    }
                    @Html.DisplayFor(model => list.Find(x => x.DepartmentId == model.DepartmentParentId).DepartmentName)
                </dd>
            </li>
        </ul>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            <div id="buttonPadding" class="col-sm-10">
                <input type="submit" value="Delete" class="btn btn-primary" />
                @Html.ActionLink("Cancel", "Index", new { }, new { @class = "btn btn-primary" })
            </div>
        }
    </div>
</div>