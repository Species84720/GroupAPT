@model IEnumerable<Group_APT.Models.Lecturer>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UniversityLecturerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Surname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LecturerId)
        </th>
        <th></th>
    </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UniversityLecturerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Surname)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LecturerId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UniversityLecturerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UniversityLecturerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UniversityLecturerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
