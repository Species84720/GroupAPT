@model IEnumerable<Test2.Models.DBModels.Student>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RelatedUser.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FacialImageDirectory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FacialImageTitle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RelatedUser.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacialImageDirectory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FacialImageTitle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StudentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StudentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StudentId })
        </td>
    </tr>
}

</table>
